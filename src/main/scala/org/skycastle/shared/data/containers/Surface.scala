package org.skycastle.shared.data.containers

/**
 * Two dimensional space that can store values with bounds at locations, and supports querying values by region.
 *
 * May have some bounds for the locations, or even be wrap around along either or both axes.
 *
 * Can detect overlap (collisions) between values?
 *
 * Provides generated Long handles for stored values that can be used to access them directly,
 * can also store entries as bound to symbols?
 */
class Surface {
  
}
