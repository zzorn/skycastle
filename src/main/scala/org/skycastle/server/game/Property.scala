package org.skycastle.server.game

/**
 * Wraps a single property that can be listened to / queried at a specific time.
 * Only transmits the most recent value of the property.
 */
trait Property {
  
}

