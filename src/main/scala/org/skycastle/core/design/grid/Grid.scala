package org.skycastle.core.design.grid

/**
 * Represents a grid.
 * Provides methods to change the grid size up or down in multiples of 2.
 * Provides a spatial with the view grid.
 * Provides picking ray detection against view squares when placing parts.
 * Might allow grids along several coordinate axes.
 * Allows moving the grid plane up or down, to work on different floors.
 */
class Grid